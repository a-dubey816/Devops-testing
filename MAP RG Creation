variable "rg_map" {
  type = map  -----> we can also use map(any)
  default = { rg1 = "west us"
    rg2 = "west europe"
  rg3 = "australiacentral" }
}
resource "azurerm_resource_group" "rgmap" {
  for_each = var.rg_map
  name     = each.key
  location = each.value
}
